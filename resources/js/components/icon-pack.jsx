import {
    Filter as FilterIcon,
    ListFilter,
    SlidersHorizontal,
} from 'lucide-react';

const IconPack = {
    Filter: FilterIcon,
    FilterFilled: SlidersHorizontal,
    FilterApplied: ListFilter,
};

export default IconPack;
